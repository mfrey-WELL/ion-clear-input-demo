import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'clear-input-test',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
